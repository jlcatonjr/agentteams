# Plan: Resolve All Remaining Refactor Items

**Branch:** `refactor/code-hygiene` (== main, 80dcfa3) · **Protocol:** commit-and-push per milestone (no PRs); behavior-preserving moves verified by the suite; behavior changes are test-first; clearance gates merge-to-main.
**Status:** DRAFT → audited (§Audit appended).

## 1. Complete inventory of remaining items (verified 2026-06-15)

| # | Item | State | Disposition |
|---|---|---|---|
| I1 | **Flaky test** `test_bridge_generate_skip_notice_cli` | Runs a bridge generate WITHOUT `--security-offline` → depends on live security intel → intermittently hits the freshness gate (RuntimeError). | **FIX** — add `--security-offline`. |
| I2 | **Two stray bridge artifacts** uncommitted (`bridge-manifest.json`, `bridge-merge.report.md`) | Diff is a `generated_at` timestamp bump + one appended bridge-merge report line — regenerated by ad-hoc/self-bridge runs this session; not hand-authored. | **REVERT** to committed state (clean the tree). |
| I3 | **jsonschema "missing" in fresh envs** | jsonschema is a **runtime dependency** (pyproject:28); `ci.yml:36` installs `-e ".[test]"` then runs pytest. The clearance sandbox's ~50 failures were its ephemeral env not installing the package. | **NO CODE ACTION** — verify ci.yml (done); document as env-setup, not a defect. |
| I4 | **`cli/app.py` = 1174 lines** (allowlisted) | Orchestrator: Layer-1 dispatch + the linear generate/update/check pipeline (with inline security gates). | **FIX (Step D2)** — extract the generate pipeline into `cli/generate.py`; app.py → <1000, off allowlist. |
| I5 | **`analyze.py` = 1503, `emit.py` = 1389** (allowlisted) | Pre-existing large modules (43 / 27 top-level funcs); untouched by the refactor. | **FIX (phased)** — split each into cohesive submodules below 1000. Largest/most open-ended item. |
| I6 | **13 broad `except` / 29 swallow** | Broad are justified external/isolation/never-block boundaries; swallow are narrow known-recoverable catches. Ratchet baselines 13/29 block new ones. | **POLISH** — ensure every broad except carries an in-code CH-24 justification; keep ratchets. |

## 2. Phased plan (each step: suite green → commit → push; clearance before merge-to-main)

### Phase 1 — Quick wins (low-risk)
- **P1a (I1):** add `--security-offline` to the two `build_team.main(...)` calls in `test_bridge_generate_skip_notice_cli` so the bridge path no longer depends on live intel. Re-run the test ×3 to confirm determinism.
- **P1b (I2):** `git checkout -- references/bridges/copilot-vscode-to-claude/bridge-manifest.json references/bridges/copilot-vscode-to-claude/bridge-merge.report.md` (revert the timestamp/report drift). Confirm `git status` clean of these.
- **P1c (I3):** no code change; record in this plan that jsonschema is a runtime dep installed by every CI workflow (verified). Done.
- **P1d (I6):** add a one-line `# CH-24:` justification to each of the 13 broad excepts that lacks one (so each is auditably intentional). No behavior change. Keep the 13/29 ratchets.

### Phase 2 — Step D2: extract the generate pipeline from `cli/app.py` (I4)
- Identify the linear generate/update/check pipeline block inside `main` (the Layer-2 region after the Layer-1 standalone-command dispatch) and extract it to `agentteams/cli/generate.py` as `run_generate(args, ...) -> int` (or the minimal set of cohesive functions), **preserving the inline security-gate ordering exactly** (gates fire before side effects).
- `main` (app.py) keeps: arg parse + `_validate_option_combinations` + Layer-1 dispatch (`--fleet`/`--self`/`--convert`/`--interop`/`--bridge`/`--migrate`/`--revert`) + a call to `run_generate(...)`.
- generate.py imports the cli/agentteams helpers it needs directly; the build_team-resident helpers (events, prune, run-log, auto-correct) reached via lazy `import build_team` (mirroring app.py). No module-level `import build_team` (no cycle).
- Verify **both** app.py and generate.py < 1000; update the CH-07 allowlist (remove app.py); full suite + CLI smokes (generate/update/check/dry-run/bridge) + the order-asserting gate behavior.

### Phase 3 — Split `analyze.py` and `emit.py` (I5)
- Map each module's top-level functions into cohesive clusters (e.g. analyze: archetype-selection, placeholder-resolution, output-file planning, tool-tier; emit: dry-run/report, section-fence merge, backup/restore, write loop).
- Extract clusters into submodules (e.g. `agentteams/analyze_*.py` or an `analyze/` package; same for emit), re-exporting names so importers (`build_team`, `render`, `cli/*`, tests) are unaffected — the proven move-and-re-export pattern. Behavior-preserving; suite green after each extraction.
- Verify each resulting module < 1000; update allowlist. This is the largest, most open-ended item — execute as several small extractions, one commit each.

## 3. Risk controls
Behavior-preserving moves verified by the full suite per commit; P1a is the only behavior change (test determinism) and is verified by re-running. The generate-pipeline extraction must not reorder the inline security gates (order-asserting test). analyze/emit splits use re-export so importers/tests/monkeypatches are unaffected; watch for circular imports (CH-13). CH-25 screen on each AI-authored diff; independent clearance before each merge-to-main.

## 4. Sequencing & merge
Phase 1 → merge to main (cleared). Phase 2 → merge to main (cleared). Phase 3 as N extractions → merge to main (cleared). Commit-and-push to `refactor/code-hygiene`; clearance gates each fast-forward of main.

## Audit — findings & revised dispositions (supersede §1–§2)

Two independent audits ran against the code. Corrections:

### CRITICAL — I1 fix as written is inert + masks a product gap
`--security-offline` is **never plumbed into the bridge path**: `_run_bridge` (commands.py:210-238) has no offline param and hardcodes `offline=False`; only the generate pipeline threads `offline=args.security_offline` (app.py:479). And offline-without-cache forces `stale` (security_refs.py:781), so the flag can't make the test pass anyway. The intermittent `RuntimeError` is a live-network dependency. **Revised:**
- **P1a (test fix):** make the test deterministic the way its **6 sibling bridge tests already do** — `monkeypatch.setattr(security_gate, "_assert_security_intelligence_fresh", lambda *_a, **_k: None)` (lines 486/530/551/591/646/709) + add the `monkeypatch` fixture. The test exercises the *skip-notice* feature, not security intel; bypassing the gate is the correct, established pattern. (Drop `--security-offline` from the test.)
- **SURFACE, don't silently change:** the bridge path ignoring `--security-offline` (so a no-network `--bridge-refresh` raises) is a real product gap. It is **security-sensitive and possibly intentional** (bridges may require live intel), so I will **report it to the user** as a newly-found item, not unilaterally alter bridge security behavior.

### I2 — split the revert (P1b)
`bridge-manifest.json` is a `generated_at`-only bump → safe `git checkout --`. `bridge-merge.report.md` appended a `recall.md` "no-rendered-fence" line — a **content/behavior signal on a safety-tracked file**. **Investigate that skip before reverting**; if it reflects a real fence-rendering change, surface it; else revert. (bridge-maintenance.yml cron regenerates the manifest but has `contents: read` + no commit step, so CI won't re-dirty; only local self-bridge runs do.)

### I4 — re-scope P2 (the cut is not "Layer-1 + linear pipeline")
Seven standalone modes (`--list/restore-backup`, `--scan-security`, `--check-budget`, `--refresh/query-index`, `--adopt-orphans`, `--check`) are **interleaved after manifest build**. Clean cut = **everything from ~app.py:285 to end** → `run_generate(args, parser) -> int` in `cli/generate.py`. Must: move `_finalize_exit_code` into generate.py (used at 944/1155); preserve inline-gate order; lazy `import build_team` for the ~10 resident calls; **verify both files <1000 BEFORE editing the allowlist** (remove app.py same-commit); do NOT add generate.py to the allowlist (it lands ~920-960, tight); add generate.py to `_REFACTOR_MODULES` + fully annotate `run_generate`. **Fallback:** if either >1000 after a behavior-preserving cut, keep app.py allowlisted.

### I5 — DEMOTE to accepted tracked debt (do not split now)
Splitting analyze.py/emit.py is the largest, riskiest item (exhaustive private-name re-export — `fence_inject.py:38` imports `_FENCE_BEGIN_RE`/`_YAML_FM_RE`; ~6 test files import private names; monkeypatch targets; CH-13 cycle risk), comparable to the whole build_team decomposition. CH-07 is **Medium** severity and the **allowlist is the rules-sanctioned disposition for justified exceptions**. **Revised: keep analyze.py/emit.py allowlisted as accepted, ratcheted debt** (new oversized modules still blocked). Revisit only when a feature must touch them. (User can request the split explicitly.)

### I6 — narrow 3, justify the rest (P1d)
Of the 13 broad excepts, **narrow** (lowers the ratchet — a win): `ingest.py:520` → `(OSError, ValueError, KeyError)` (also a swallow); `memory_index_incremental.py:367` → `jsonschema.ValidationError` (matches adjacent line 361); `mcp_emit.py:114` → `(OSError, ValueError)`. Add `# CH-24:` justification to the genuine boundaries (app.py:80/506, convert.py:278, fleet.py:354, handoff_payloads.py:124, plan_steps_todo.py:164 [cleanup-then-reraise], build_team.py:251/305/385 [never-block-emit], scripts/verify-env.py:131). Lower the broad baseline 13 → 10. Each narrowing is behavior-changing on the error branch → verify by suite.

### I3 — confirmed non-issue (no action).

### Sequencing & clearance (conflict #6)
Batch **Phase 1 (I1 test fix + I2 + I6) + Phase 2 (I4 extraction)** behind **one** independent clearance + one main fast-forward (matches the prior "group of steps, one clearance" cadence). I5 demoted. Net plan: P1a(monkeypatch) → I6(narrow+justify) → I2(revert/investigate) → I4(generate.py extraction) → clearance → merge; surface the bridge-offline product gap to the user.
